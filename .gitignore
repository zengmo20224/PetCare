# OS and editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# Java / Spring Boot
target/
*.class
*.jar
*.war
*.ear
.mvn/wrapper/maven-wrapper.jar

# Node / Vue / uni-app
node_modules/
dist/
build/
.output/
.vite/
coverage/

# Environment and secrets
.env
.env.*
!.env.example
!.env.development
!.env.production
application-local.yml
application-local.yaml

# Database and uploaded runtime files
*.db
*.sqlite
uploads/
data/

# Docker local state
docker-compose.override.yml

# Docker runtime volumes（防止误提交容器数据）
mysql-data/
*.tar

# Maven local repo（Jenkins 流水线内 .m2-repo）
.m2-repo/

# CI 测试报告产物（不进版本库，由 CI 归档）
target/site/jacoco/
frontend/*/coverage/

# 审计证据（图片/视频可能较大，按需手动归档）
docs/audit-evidence/**/*.mp4
docs/audit-evidence/**/*.mov

# Agent/工具临时产物（不纳入版本库）
.claude/
.zcode/
docgen/

# 微信小程序个人配置（含 appid / 本地路径，不应共享）
# 项目当前主目标为 H5，小程序工程配置暂不纳入版本库（AGENTS.md §2）
project.private.config.json
project.config.json
**/project.private.config.json
**/project.config.json

# 误产物（Windows nul 是保留设备名，不应作为文件存在）
nul

# 一次性交付/课程文档（二进制，体积大，不纳入版本库；本地保留）
*.docx
*.pptx
*.zip

# 本地临时截图目录
tmp-ui-shots/
