# AGENTS-TEMPLATE —— 项目方法论模板

> 本模板从 PetCare O2O 项目（2026-06 ~ 2026-08，单人 + AI Agent 协作交付 1093+ 测试的全栈项目）沉淀而来，可直接复制为新项目的 `AGENTS.md` 并按【】占位符替换。
>
> 核心理念：**用最少的流程成本，持续交付可运行、可验证的产品切片；把重要规则写成文档的同时，尽量再写成会失败的测试。**

---

## 0. 本项目验证有效的方法论（为什么这样设计）

以下每条都在 PetCare O2O 中经受过实战检验，新项目按需裁剪：

1. **纵向切片 > 横向分层**：每个任务交付一个端到端可验收的功能切片（API + 前端 + 管理能力 + 测试），而不是"先写完所有 Mapper 再写 Service"。M1–M8 里程碑全部按此推进，无一次返工重写。
2. **边界文档先行**：项目第一天就写定"范围 + 不在范围"清单（`00-project-boundary`）和需求基线，此后所有"要不要做 X"的争论都引用文档而非重新讨论。
3. **决策记录（Decision Log）**：每个影响架构的决定记录 ID、理由、日期、修订史和**回退条件**。项目后期（如 D-014 拒绝 Dify）能直接引用两个月前的决策依据，避免重复论证。
4. **架构守卫测试**：把"AI 不许直连数据库"这类文档规则写成反射扫描测试（`*ArchitectureTest`），违规提交会直接挂 CI——**文档会过时，测试不会**。
5. **风险驱动测试**：只为高风险规则（并发、金额、库存、状态机、权限、隐私）写直接测试并要求覆盖率，不为凑数写测试；诊断型测试在永久守卫就位后归档（`docs/test-archive/`）。
6. **文档极简**：只维护一份执行规则（AGENTS.md）+ 一组带编号的核心文档；禁止阶段性 brief/review/handoff 文档；调研文档在结论落地后合并为决策记录并删除原文。
7. **真实演示数据**：演示环境从不用静态假数据——种子 SQL + 授权媒体资产 + 幂等迁移脚本，任何时刻可一键复现完整演示。
8. **定期安全审计**：里程碑间插入白盒审计（代码 + 部署配置 + git 历史密钥扫描），发现项进文档清单跟踪到关闭，不靠记忆。
9. **降级路径全覆盖**：外部依赖（LLM、向量库、支付）全部设计 disabled/mock/real 三态和失败降级，演示永不被外部服务故障卡死。
10. **验证后才交接**：交接模板强制列出验证证据；"没跑过测试不得声称完成"。

---

## 1. 开工前只读检查（复制即用）

每次任务开始时按顺序阅读：

1. `AGENTS.md`（本文件）
2. `README.md`
3. 【项目边界文档，如 docs/00-project-boundary.md】
4. 【任务路线图，如 docs/02-task-breakdown.md】
5. 与当前任务直接相关的代码和文档
6. 仅当任务涉及未决事项时读【决策记录，如 docs/08-pending-decisions.md】

修改文件前运行：

```bash
git status --short --branch
git log --oneline -5
```

随后简要汇报：当前目标、修改范围、不修改范围、风险、验证方式。不重复汇报已知历史。

## 2. 产品定位（新项目必填）

- 【一句话产品定位 + 首发终端，例：响应式 H5 优先，小程序延后不阻塞】
- 【必须"真实可用"的模块清单，例：商品/购物车/订单必须走真实后端规则】
- 【明确简化的模块清单，例：社区只留发帖/点赞/评论】
- 【外部能力分阶段策略，例：AI 分 V1 客服 / V2 Agent 两阶段，每阶段先写安全边界】
- 【架构形态约束，例：V1 保持模块化单体，不改微服务】

## 3. 快速交付方式

- 一次任务领取**一个可端到端验收的纵向切片**，不是极小文件或单接口。
- 直接完成"分析 → 实现 → 测试 → Review 修复 → 交接"，不为每步新增独立计划文档。
- 无真实阻塞时持续推进到可验证结果；有阻塞或未决事项才停下请求决定。
- 优先复用现有实现；不做无关重构、不随意改名、不重复造轮子。
- 文档只维护本文件 + `docs/` 核心集；**不创建阶段性 brief/review/handoff 文档**。
- 有意义的已验证切片形成一个提交；禁止为展示过程强制制造碎片提交。
- 禁止直接向远程推送/合并，除非用户明确授权。

## 4. 工程底线（红线，违反即缺陷）

- 不覆盖、回退或删除用户及其他 Agent 的未提交变更；禁止 `git reset --hard` / `git clean -fd` / `git checkout -- .`。
- 所有外部输入必须校验；错误响应不泄露 SQL、堆栈、密钥或上游原始错误。
- 密码、Token、API Key、数据库凭据不硬编码不入库。
- Controller 不直接调用 Mapper/DataAccess 完成业务流程。
- 【多表状态变更/资金/库存/容量类操作】必须服务端事务 + 服务端校验，锁顺序全局统一。
- 【AI/自动化能力类约束，例：AI 不直连业务库，只经受限 Tool → 业务 Service；不做诊断/处方/承诺；建议不自动改数据】——并落到架构守卫测试。

## 5. 风险驱动测试

- Bug 修复和高风险规则：先写失败测试，再实现修复。
- 简单 UI/文档/配置：直接实现后验证。
- 关键变更逻辑覆盖率 ≥【80%】；全仓覆盖率只作趋势指标不作任务门禁。
- 先跑受影响模块的快速测试；发布前再全量构建 + 测试 + 关键 E2E。
- 只改文档时检查引用、格式、Git 差异即可。
- **诊断型测试生命周期**：临时测试在永久回归守卫覆盖后归档（记录验证了什么、缺陷 ID、由哪个保留测试接管），然后从套件删除。永久守卫（并发/金额/状态机/权限/隐私/AI 边界）永不删除。
- 测试命令清单见【docs/05-testing-and-verification.md】。

## 6. Git 与变更范围

- 不在主干直接开发业务功能；已有工作分支可续用，不为小任务新建分支。
- 修改前后检查工作区；只动当前任务所需文件。
- 提交前运行相关验证 + `git diff --check` + `git status --short --branch`。
- 不自行改变核心业务状态、字段、API 或数据库规则；需要改变时先说明影响。
- 提交信息用 Conventional Commits：`<type>(<scope>): <描述>`。

## 7. 未决事项管理

- 只有决策记录中标记"未决"**且直接影响当前任务**的事项才阻塞实现。
- 决定完成后直接更新决策记录和相关核心文档，不新增独立决策文档。
- 已决定的事项及时移出未决区——**不让"僵尸未决项"累积**（定期清理）。
- 技术实现细节由 Agent 依现有代码保守选择，不升级为未决事项。

## 8. 完成交接（模板）

```text
目标：
已完成：
变更文件：
验证：
未完成或风险：
下一步：
```

**没有验证证据时，不得声称功能已完成、已可用或已接入。**

## 9. 文档优先级

```text
用户最新明确指令
> AGENTS.md
> 需求基线（requirements-source）
> 项目边界（00-boundary）
> 架构设计（01-architecture）
> 任务路线图（02-task-breakdown）
> 其他核心文档
```

## 10. 新项目初始化清单

1. 复制本模板为 `AGENTS.md`，填【】占位符，删掉不适用的章节。
2. 建立 `docs/` 编号体系：00 边界 / 01 架构 / 02 路线图 / 03+ 按需（配置管理、测试、构建、部署、决策记录）。
3. 第一天就写"范围 + 不在范围"清单；第一个里程碑前建立种子数据机制。
4. CI 先行：push 即跑构建 + 快速测试，覆盖率随切片增长而非事后补。
5. 需要配置管理/课程交付场景时，按 IEEE 828 建 CMP + 配置项登记表 + 基线 tag + 变更申请单流程。
6. 引入 AI 能力前，先写安全边界文档 + 架构守卫测试，再写功能。
